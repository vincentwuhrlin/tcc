split ✅ ← on est là — 5 035 chunks structurés
discover → tags + summary par chunk
synthesize → vue d'ensemble par source
classify → catégorisation dans l'INDEX.md
bundle → pExactement, le split c'est la fondation — et elle est propre. Le pipeline fait le reste :

1. **split** ✅ ← on est là — 5 035 chunks structurés
2. **discover** → tags + summary par chunk
3. **synthesize** → vue d'ensemble par source
4. **classify** → catégorisation dans l'INDEX.md
5. **bundle** → packing en bundles pour Claude Projects

On passe au `media:discover` ?